#include<stdio.h>
void main(){
    int num,n,dig,sum=0;
    scanf("%d",&num);
    n=num;
    while(n!=0){
    dig=n%10;
    sum=sum+(dig*dig*dig);
    n=n/10;
    }
    if(num==sum){
        printf("%d armstrong number",num);
    }
    else
{
    printf("%d not armstrong number",num);
}
}
